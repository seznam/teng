#!/usr/bin/make -f

DH_VERBOSE=1

%:
	dh $@ --with autoreconf --parallel

override_dh_auto_configure:
	dh_auto_configure -- --enable-optimization --disable-debug

override_dh_strip:
	dh_strip --dbg-package=libteng4-dbg

.PHONY: override_dh_strip

